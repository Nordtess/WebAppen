<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img" aria-label="Closed envelope">
  <!-- Closed envelope -->
  <g fill="none" stroke="#000" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
    <!-- Envelope body (filled white) -->
    <rect x="16" y="22" width="32" height="20" rx="3.5" fill="#fff"/>
    <!-- Flap (V-shape to the center) -->
    <path d="M16 24 L32 34 L48 24"/>
  </g>
</svg>
